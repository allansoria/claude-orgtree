spike/results/
# the maintainer's working todo list — deliberately local, never published
docs/todos.md
__pycache__/
node_modules/
dist/
# hidden production source maps: emitted for server-side crash-report
# resolution (crashreports.resolve_stack) and moved here, OUT of dist/, by
# frontend/scripts/postbuild-sourcemaps.mjs. Build output, never committed.
sourcemaps/
.venv/
shots*/

# machine-local hub config (compose reads it automatically)
hub/.env

# machine-local secrets (OpenRouter recon, GitHub token)
.env

# run.ps1 detached-server pid tracking
.run-pids
