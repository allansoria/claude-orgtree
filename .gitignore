spike/results/
# the maintainer's working todo list — deliberately local, never published
docs/todos.md
__pycache__/
node_modules/
dist/
.venv/
shots*/

# machine-local hub config (compose reads it automatically)
hub/.env

# machine-local secrets (OpenRouter recon, GitHub token)
.env

# run.ps1 detached-server pid tracking
.run-pids
